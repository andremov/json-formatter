type LocaleStrings = {
  error: string;
  title: string;
  labels: {
    data: string;
    input: string;
    output: string;
  };
  buttons: {
    copy: string;
    copied: string;
    minimize: string;
    expand: string;
  };
};

export default LocaleStrings;
